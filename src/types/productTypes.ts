export type Product = {
  id: string;
  productId: string;
  title: string;
  description?: string;
  amount?: number;
  featuredImage?: string;
  quantity: number;
  size?: string;
  color?: string;
};
